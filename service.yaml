apiVersion: v1
kind: Service
metadata:
  name: hello-devops-service
spec:
  selector:
    app: hello-devops
  ports:
    - protocol: TCP
      port: 32777
      targetPort: 32777
      nodePort: 30077
  type: NodePort